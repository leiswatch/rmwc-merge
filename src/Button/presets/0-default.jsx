import * as React from "react";
import Button from "../Button";

export default (
  <Button uxpId="1" ripple raised style={{ width: "100%" }}>
    Click me because this is release 2.5 TEST
  </Button>
);
